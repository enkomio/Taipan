image: Visual Studio 2015

version: '{build}'

install:
  - cinst fake -pre
  - cinst paket

build_script:
  - cmd: build.bat

test_script:
    - cmd: Src\build\EndToEndTests\EndToEndTests.exe
  
artifacts:
  - path: Src\deploy
